import { NumbersToHidePipe } from './numbers-to-hide.pipe';

describe('NumberToHidePipe', () => {
  it('create an instance', () => {
    const pipe = new NumbersToHidePipe();
    expect(pipe).toBeTruthy();
  });
});
