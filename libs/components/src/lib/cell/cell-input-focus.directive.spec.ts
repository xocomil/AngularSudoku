import { CellInputFocusDirective } from './cell-input-focus.directive';

describe('CellInputFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new CellInputFocusDirective();
    expect(directive).toBeTruthy();
  });
});
