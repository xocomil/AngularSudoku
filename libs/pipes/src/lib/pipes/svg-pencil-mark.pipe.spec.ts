import { SvgPencilMarkPipe } from './svg-pencil-mark.pipe';

describe('SvgPencilMarkPipe', () => {
  it('create an instance', () => {
    const pipe = new SvgPencilMarkPipe();
    expect(pipe).toBeTruthy();
  });
});
